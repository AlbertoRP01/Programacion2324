/*
package Relacion1_1;

import java.util.Scanner;

public class Ejercicio8 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        do {
            System.out.println("Introduce una hora");
            int hora = Integer.parseInt(sc.nextLine());

            System.out.println("introduce los minutos");
            int min = Integer.parseInt(sc.nextLine());

            System.out.println("Introduce los segundos");
            int seg = Integer.parseInt(sc.nextLine());

            System.out.println("Introduce la segunda hora ");
            int hora1 = Integer.parseInt(sc.nextLine());

            System.out.println("Introduce los minutos");
            int min1 = Integer.parseInt(sc.nextLine());
            System.out.println("Introduce los segundos");
            int seg1 = Integer.parseInt(sc.nextLine());


        } while (hora < 0 && hora1 > 23 && min < 0 && min1 > 59 && seg < 0 && seg1 > 59);

        if (hora1 > hora2) {
            System.out.println("la hora" + hora + min + seg + "es mayor que" + hora1 + min1 + seg1);
        } else if (hora1 > hora) {
            System.out.println("la hora " + hora1 + min1 + seg1"es mayor que" + hora + min + seg);

        } else if (min > min1) {
            System.out.println("la hora " + hora + min + +seg"es mayor que " + hora1 + min1 + seg1);

        } else if {
            if (min1 > min) {
                System.out.println("la hora " + hora1 + min1 + seg1"es mayor que " + hora + min + seg);

            } else {
                if (seg > seg1) {
                    System.out.println("La hora " + hora + min + seg"es mayor que " + hora1 + min1 + seg1);
                } else if (seg1 > seg) {
                    System.out.println("la hora " + hora1 + min1 + seg1"es mayor que" + hora + min + seg);

                }
            }
        }
    }
}
*/
